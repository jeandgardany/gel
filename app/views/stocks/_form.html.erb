<%= form_with(model: stock, local: true) do |form| %>
  <% if stock.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(stock.errors.count, "error") %> prohibited this stock from being saved:</h2>

      <ul>
      <% stock.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :amount, "Quantidade" %>
    <%= form.number_field :amount, id: :stock_amount %>
  </div>

  <div class="field">
    <%= form.label :laboratory_id, "Laboratório" %>
    <%= form.text_field :laboratory_id, id: :stock_laboratory_id %>
  </div>

  <div class="field">
    <%= form.label :product_id, "Produto" %>
    <%= form.text_field :product_id, id: :stock_product_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
