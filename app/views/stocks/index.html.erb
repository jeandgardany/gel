<p id="notice"><%= notice %></p>

<h1>Estoque</h1>

<table>
  <thead>
    <tr>
      <th>Laboratório</th>
      <th>Produto</th>
      <th>Quantidade</th>      
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @stocks.each do |stock| %>
      <tr>
        <td><%= stock.laboratory %></td>
        <td><%= stock.product %></td>
        <td><%= stock.amount %></td>
        <td><%= link_to 'Mostrar', stock %></td>
        <td><%= link_to 'Editar', edit_stock_path(stock) %></td>
        <td><%= link_to 'Apagar', stock, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to t('.new', :default => t("Nova Entrada")),
            new_input_path, :class => 'btn btn-primary' %>
<%= link_to t('.new', :default => t("Nova Saída")),
            new_exit_path, :class => 'btn btn-primary' %>
<%= link_to t('.new', :default => t("Voltar")),
            home_index_path, :class => 'btn btn-primary' %>
