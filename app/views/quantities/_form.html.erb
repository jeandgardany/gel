<%= form_with(model: quantity, local: true) do |form| %>
  <% if quantity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(quantity.errors.count, "error") %> prohibited this quantity from being saved:</h2>

      <ul>
      <% quantity.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.text_field :product_id, id: :quantity_product_id %>
  </div>

  <div class="field">
    <%= form.label :amount %>
    <%= form.number_field :amount, id: :quantity_amount %>
  </div>

  <div class="field">
    <%= form.label :reserve_id %>
    <%= form.text_field :reserve_id, id: :quantity_reserve_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
