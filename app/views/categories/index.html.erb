<p id="notice"><%= notice %></p>

<h1>Categories</h1>

<table>
  <thead>
    <tr>
      <th>Nome</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @categories.each do |category| %>
      <tr>
        <td><%= category.name %></td>
        <td><%= link_to 'Mostrar', category, class: "btn btn-primary btn-sm" %></td>
        <td><%= link_to 'Editar', edit_category_path(category), class: "btn btn-primary btn-sm" %></td>
        <td><%= link_to 'Apagar', category, method: :delete, data: { confirm: 'Você tem certeza?' }, class: "btn btn-primary btn-sm" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nova Categoria', new_category_path, class: "btn btn-primary btn-sm" %>
