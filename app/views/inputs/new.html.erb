<h1>Nova Entrada</h1>

<%= render 'form', input: @input %>

<%= link_to t('.new', :default => t("Voltar")),
            stocks_path, :class => 'btn btn-primary btn-sm' %>
<%= link_to t('.new', :default => t("Entradas")),
            inputs_path, :class => 'btn btn-primary btn-sm' %>
