<%= form_with(model: laboratory, local: true) do |form| %>
  <% if laboratory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(laboratory.errors.count, "error") %> prohibited this laboratory from being saved:</h2>

      <ul>
      <% laboratory.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :code, "Código" %>
    <%= form.number_field :code, id: :laboratory_code %>
  </div>

  <div class="field">
    <%= form.label :name, "Nome" %>
    <%= form.text_field :name, id: :laboratory_name %>
  </div>

  <div class="field">
    <%= form.label :description, "Descrição" %>
    <%= form.text_area :description, id: :laboratory_description %>
  </div>

  <div class="field">
    <%= form.label :capacity, "Capacidade" %>
    <%= form.number_field :capacity, id: :laboratory_capacity %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
